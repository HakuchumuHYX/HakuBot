# HakuBot Go 重写目标

将 `/opt/HakuBot` 从 Python / NoneBot 迁移到 Go / ZeroBot。

Python 版是梳理现有行为、配置和数据格式的参考；Go 版按实际需求实现，不机械照搬 Python 的目录和抽象。

## 分阶段推进

1. 梳理 Python 版结构：画出结构树，说明入口、核心模块、公共工具、插件及配置和数据的位置，理清调用关系。
2. 确定依赖：根据现有功能确认 ZeroBot 的能力、必要的 Go 库和外部工具，标明需要替代或暂时无法覆盖的能力。
3. 搭建通用框架：围绕已确认的业务需求实现启动装配、配置、路径和公共工具等基础能力。
4. 实现具体业务：逐个迁移插件和功能，对照 Python 版确认命令、权限、消息处理和业务行为。
5. 迁移数据与接线：明确新旧数据格式和路径，完成必要的转换、功能装配及部署配置，使 Go 版可以实际接替原项目。

- 这是持续推进的重写，不要求一次完成。每次围绕用户指定的阶段或功能开展工作，简要说明完成内容和剩余事项。
- 阶段用于组织工作，不设置审批关卡、复杂任务体系或重复的计划与验收文档；发现实际依赖时可以调整先后顺序。
- 不提前生成整套插件骨架、空目录或尚无使用方的基础设施。目录和包边界在梳理后按实际职责确定。

## 迁移边界

- 迁移插件时一并原样复制对应的真实配置到 Go 工作区，不输出密钥内容，不纳入版本管理。已有不同配置时先核对，避免覆盖用户修改；配置示例放在 `config_example/`，采用 4 空格缩进，真实配置放在 `config/`。

- 默认在本工作区实现重写，不顺带修改 `/opt/HakuBot` 的代码、配置或运行数据。
- 数据迁移依据实际格式差异实施。普通文件整理直接移动文件并修改引用；确需格式转换时使用最小必要的转换工具，不建设通用迁移框架，也不设置启动迁移门槛。
- 数据迁移准备与正式切换分开处理；覆盖或删除原始运行数据、启停服务、向群发送消息等操作仅按用户明确授权执行。

## 当前进度（2026-09-09）

- Python 版结构梳理已落到 `ARCHITECTURE.md`。
- 已安装并锁定 ZeroBot 及公共渲染、图片、Markdown、JSON 修复依赖。
- 已建立 Go 启动入口、core 和 utils 的公共实现；当前功能与 Python 对应边界见 `README.md`。
- `ai_assistant` 已迁移并接入启动入口，保留对话、搜索、生图、视觉提炼和模型管理能力；构建已通过，尚未进行实际服务联调。
- `alive_stat` 代码迁移已完成并接入启动入口；真实配置和原字体已复制，绘图复用公共层并保留原布局、配色与样式。Autochat 逻辑保持原样。构建已通过，尚未进行实际服务联调。统计文件留到正式切换时直接移动，不建设兼容层或迁移工具。
- `ai_assistant`、`alive_stat` 的真实配置已从 Python 版原样复制；`config/bot.json` 已根据 Python 环境配置与 OneBotFilter 路由生成，保留双命令前缀。真实配置由 Git 忽略。运行数据尚未搬入，未连接现有 Bot；其他插件配置进度见各插件条目。
- `analysis_bilibili` 代码迁移已收尾并接入启动入口，保留视频、番剧、直播间、专栏和动态的自动解析与可选视频搜索。真实配置按原版默认值新建，配置示例已建立。审核反馈已修复，最终复审未发现新的需修复问题；构建、格式检查和 `git diff --check` 通过，尚未进行实际接口与 OneBot 联调。去重、WBI 密钥与 ticket 均为内存状态，无持久化数据需要搬迁。
- `bili_dyn_sub` 迁移已结束并接入启动入口，保留订阅、退订、列表、测试命令，以及凭据刷新、登录提醒、轮询退避、持久化去重、文字卡片和 3×N 拼图分发。真实配置已从 Python 版原样复制到 `config/plugins/bili_dyn_sub/config.json`，已核对内容一致并受 Git 忽略；配置示例已建立。审核反馈已修复，最终复审未发现新的需修复问题，构建、格式检查和 `git diff --check` 通过。Go 直接读写 Python 版 `state.json` 与 `credential.json` 格式，用户后续复制 `data/bili_dyn_sub` 即可沿用原数据，无需导入或转换。尚未进行 B 站接口与 OneBot 实际联调。

- `atri_reply` 已处理：按用户决定不迁移，后续跳过该插件。

## 后续联调与已知限制

- 已迁移插件仍需在正式接线阶段验证实际接口、Cookie 与 OneBot 消息发送；代码迁移收尾不表示已完成线上联调。
- 当前 ZeroBot WebSocket 驱动在等待响应前同步写入 socket，写入阶段没有 deadline。插件发送使用的 30 秒 context 限制响应等待并继承应用取消，但不能保证底层写入的时间上限；此项作为依赖层问题单独跟进。

## 已确定的功能取舍

- `add_friend`：不迁移到 Go 版。保留 Python 主项目源码，不新增对应业务插件。
- `atri_reply`：不迁移到 Go 版。保留 Python 主项目源码，不迁移对应代码、配置或数据。
- `ai_assistant`：保留全部现有功能，包括多模态对话、回复与合并转发解析、自动/强制联网、查询重写、文生图/图片编辑、联网生图的视觉设定提炼与提示词编译、模型切换、回复渲染以及开关/CD。精简限于代码结构、重复逻辑和实现方式，不通过删除功能或配置能力缩减规模；迁移时修正已确认的实现问题。
- `alive_stat`：保留全部现有功能，包括 `alive` / `alive day` / `alive night`、群开关与超管豁免、HakuBot 运行时长统计、Autochat 只读时长、CPU/内存/Swap/磁盘采集、进程与 Docker 监控、ping、日夜状态卡，以及每五分钟和退出时保存。不增加 CD、持续轮询或在线告警。进程关键词不自动改写；统计文件不在迁移阶段复制，正式切换时直接移动。
- `analysis_bilibili`：保留全部解析与格式化能力（视频、番剧、直播、专栏、动态），保留可选 `搜视频` 命令。默认只发送文字、关闭重复限制、关闭显式搜索、直连网络。使用 ZeroBot 事件模型，图片按需限量下载并使用 `ImageBytes`，支持 OneBot 拒绝时的纯文字降级。去重基于会话维度与标准 URL，内存 TTL 过期清理；WBI 与 ticket 独立带锁缓存。不引入外部数据库或独立后台任务。
- `bili_dyn_sub`：保留全部 B 站动态订阅、退订、列表查询与测试能力，严格维持超管专用权限与订阅开关语义（不接入普通群开关或 CD）。底层采用双层凭据架构与独立退避管理，无缝读取并持久化 Python 版 `state.json` 与 `credential.json`。图文渲染沿用公共 Chromium 实例与模板，图片拼合沿用 3×N 网格逻辑与合并转发降级策略。
