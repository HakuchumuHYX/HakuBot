# HakuBot 项目约定

这是一个使用 Go / ZeroBot 的自用 Bot 项目。修改应直接解决当前需求，保持代码轻量、清晰，避免引入不必要的工程流程。

## 轻量设计与可读性

- 禁止过度设计。简单逻辑直接实现，不为假想的未来扩展增加接口、工厂、适配层、注册机制或自建框架。
- 优先使用 Go 标准库和 ZeroBot 已有能力；引入依赖应有当前功能的明确需要，不重复封装框架已有功能。
- 多个实际使用方需要的公共能力集中维护；业务规则留在所属功能中。公共工具不反向依赖具体业务。
- 不提前生成整套插件骨架、空目录或尚无使用方的基础设施。
- 按实际复杂度拆分文件和包，不强制所有功能套用同一目录模板，不把无关代码堆进 `utils`。
- 使用清晰的命名和直接的控制流，保持函数职责集中；不为减少行数牺牲可读性，也不把简单流程拆成大量跳转。
- 已有视觉模板、CSS 和界面设计应沿用；插件负责内容与样式，通用渲染层负责转换和渲染，不自行重设计或把页面样式散落在 Go 字符串中。
- 注释适量，解释必要的业务背景、非直观约束和设计原因；不逐行复述代码，不堆砌模板注释或显而易见的说明。
- Go 代码使用 `gofmt` 和惯用命名。接口调整时同步修改调用方，清理废弃入口和重复实现，不保留无实际需要的兼容层。

## 禁止过度防御

- 在配置、外部输入、网络、文件和数据库等边界处理实际可能出现的错误；内部代码遵循明确约定，不逐层重复校验。
- 不添加无依据的默认值、静默吞错、层层兜底、无限重试或全局 `recover` 来掩盖问题。必要的错误应带有足以定位问题的上下文。
- 不引入跨插件配置回退，不读取其他插件的密钥、模型或业务设置补缺。
- 保留现有行为所需的容错，不以精简为由删除仍有实际用途的错误处理或功能。

## 禁止测试文件与重流程

- 不新增测试文件（包括 `*_test.go`）、测试目录、测试脚本、mock、fixture、性能基准或测试专用依赖。
- 不创建临时测试副本、沙箱工作区或验收脚本，不主动运行测试套件，不搭建额外的验证体系。
- 通过阅读代码、查找实际引用和必要的构建或运行日志确认修改。已有可构建代码时可运行 `go build ./...`；文档修改不需要构建。
- 不为普通修改引入复杂 CI、审批流程、回滚框架或完成标记。

## 配置、数据与修改边界

- 保留用户已有改动，不重置工作区。
- 保持现有配置内容不变，除非当前任务明确要求修改。读取配置不得自动补字段、删字段或回写标准化结果；管理命令修改设置时保留其他字段，包括未知字段。
- 覆盖或删除运行数据、启停服务、向群发送消息等操作仅按用户明确授权执行。
- 配置示例统一放在 `config_example/`，使用 4 空格缩进；真实配置放在 `config/`，两者保持相同的相对路径。
- 不提交密钥、`.env` 私密配置、运行数据、数据库、缓存和日志。依赖文件与可公开的配置示例可以纳入版本管理。
- 说明与提交信息保持简明，写清实际改动、已做的检查和未完成的部分；未运行的功能不声称已验证。
